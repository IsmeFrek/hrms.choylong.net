const fs=require('fs');const p='d:/DB/web_V4/src/pages/ReplayfilePage.jsx';const s=fs.readFileSync(p,'utf8');const open=(s.match(/<div[\s>]/g)||[]).length;const close=(s.match(/<\/div>/g)||[]).length;console.log('<div> open',open,'close',close);const openspan=(s.match(/<span[\s>]/g)||[]).length;const closespan=(s.match(/<\/span>/g)||[]).length;console.log('<span> open',openspan,'close',closespan);console.log('total tags diff', open-close);