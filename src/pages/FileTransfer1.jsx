import React from 'react';
import FileTransfer from './FileTransfer.jsx';

// FileTransfer1 is intentionally the same UI/UX as FileTransfer
export default function FileTransfer1() {
  return <FileTransfer />;
}
